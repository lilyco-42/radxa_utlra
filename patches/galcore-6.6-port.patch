diff --git a/drivers/galcore/Makefile b/drivers/galcore/Makefile
index 1356d6e..62882ca 100644
--- a/drivers/galcore/Makefile
+++ b/drivers/galcore/Makefile
@@ -7,7 +7,7 @@ HOST := $(shell hostname)
 EXTRA_CFLAGS += -DHOST=\"$(HOST)\"
 
 EXTRA_CFLAGS += \
-	-DNO__DMA_COHERENT=1 -Werror -Wno-implicit-fallthrough -Wno-unused-variable -DLINUX -DDRIVER \
+	-DNO__DMA_COHERENT=1 -Wno-implicit-fallthrough -Wno-unused-variable -DLINUX -DDRIVER \
 	-DDBG=0 -DUSE_PLATFORM_DRIVER=1 -DVIVANTE_PROFILER=1 -DVIVANTE_PROFILER_CONTEXT=1 \
 	-DENABLE_GPU_CLOCK_BY_DRIVER=0 -DUSE_NEW_LINUX_SIGNAL=0 -DUSE_LINUX_PCIE=0 \
 	-DgcdCACHE_FUNCTION_UNIMPLEMENTED=0 -DgcdENABLE_3D=1 -DgcdENABLE_2D=0 \
diff --git a/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_dmabuf.c b/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_dmabuf.c
index 44fbfac..211e979 100644
--- a/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_dmabuf.c
+++ b/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_dmabuf.c
@@ -119,12 +119,14 @@ dma_buf_info_show(struct seq_file *m, void *data)
     list_for_each_entry(buf_desc, &priv->buf_list, list) {
         struct dma_buf *buf_obj = buf_desc->dmabuf;
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 6, 0)
         ret = mutex_lock_interruptible(&buf_obj->lock);
 
         if (ret) {
             seq_puts(m, "ERROR locking buffer object: skipping\n");
             continue;
         }
+#endif
 
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(3, 10, 0)
         exp_name = buf_obj->exp_name;
@@ -144,7 +146,9 @@ dma_buf_info_show(struct seq_file *m, void *data)
         size += buf_obj->size;
         npages += buf_desc->npages;
 
+#if LINUX_VERSION_CODE < KERNEL_VERSION(6, 6, 0)
         mutex_unlock(&buf_obj->lock);
+#endif
     }
 
     seq_printf(m, "\nTotal %d objects, %d pages, %zu bytes\n", count, npages, size);
diff --git a/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_gfp.c b/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_gfp.c
index 09902c0..93651b5 100644
--- a/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_gfp.c
+++ b/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_gfp.c
@@ -382,6 +382,10 @@ _GFPAlloc(gckALLOCATOR Allocator, PLINUX_MDL Mdl, gctSIZE_T NumPages, gctUINT32
     gctSIZE_T i = 0;
     gctBOOL contiguous = Flags & gcvALLOC_FLAG_CONTIGUOUS;
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 4, 0)
+#ifndef __GFP_ATOMIC
+    /* Allwinner 6.6 BSP gfp headers dropped __GFP_ATOMIC. */
+    #define __GFP_ATOMIC 0
+#endif
     u32 normal_gfp = __GFP_HIGH | __GFP_ATOMIC | __GFP_NORETRY | gcdNOWARN;
 #else
     u32 normal_gfp = __GFP_HIGH | __GFP_NORETRY | gcdNOWARN;
@@ -728,7 +732,11 @@ _GFPMmap(gckALLOCATOR Allocator, PLINUX_MDL Mdl, gctBOOL Cacheable,
 
     gcmkHEADER_ARG("Allocator=%p Mdl=%p vma=%p", Allocator, Mdl, vma);
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 3, 0)
+    vm_flags_set(vma, gcdVM_FLAGS);
+#else
     vma->vm_flags |= gcdVM_FLAGS;
+#endif
 
     if (Cacheable == gcvFALSE) {
         /* Make this mapping non-cached. */
diff --git a/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_reserved_mem.c b/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_reserved_mem.c
index 1c92af1..e6f39ad 100644
--- a/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_reserved_mem.c
+++ b/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_reserved_mem.c
@@ -227,7 +227,11 @@ reserved_mem_mmap(gckALLOCATOR Allocator, PLINUX_MDL Mdl, gctBOOL Cacheable,
     pfn = (res->start >> PAGE_SHIFT) + skipPages;
 
     /* Make this mapping non-cached. */
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 3, 0)
+    vm_flags_set(vma, gcdVM_FLAGS);
+#else
     vma->vm_flags |= gcdVM_FLAGS;
+#endif
 
 #if gcdENABLE_BUFFERABLE_VIDEO_MEMORY
     vma->vm_page_prot = pgprot_writecombine(vma->vm_page_prot);
diff --git a/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_user_memory.c b/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_user_memory.c
index 4d87117..d73a25f 100644
--- a/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_user_memory.c
+++ b/drivers/galcore/os/allocator/default/gc_hal_kernel_allocator_user_memory.c
@@ -193,6 +193,28 @@ phy_is_from_reserved(gckOS Os, gctPHYS_ADDR_T Physical)
     return status;
 }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0)
+/*
+ * Port note (6.6.98-4-aw2511): this kernel does not export
+ * __pte_offset_map_lock(), so pte_offset_map_lock() cannot be used by a
+ * module. Open-code the 5.15-equivalent using static-inline helpers only.
+ * Callers have already validated pmd_none()/pmd_bad().
+ */
+static inline pte_t *
+_galcore_pte_offset_map_lock(struct mm_struct *mm, pmd_t *pmd,
+                             unsigned long addr, spinlock_t **ptlp)
+{
+    pte_t *pte = pte_offset_kernel(pmd, addr);
+
+    *ptlp = pte_lockptr(mm, pmd);
+    spin_lock(*ptlp);
+
+    return pte;
+}
+#define pte_offset_map_lock(mm, pmd, addr, ptlp) \
+    _galcore_pte_offset_map_lock((mm), (pmd), (addr), (ptlp))
+#endif
+
 static int
 import_page_map(gckOS Os, struct device *dev, struct um_desc *um,
                 unsigned long addr, size_t page_count, size_t size, unsigned long flags)
@@ -211,19 +233,25 @@ import_page_map(gckOS Os, struct device *dev, struct um_desc *um,
 
     down_read(&current_mm_mmap_sem);
 
-#if LINUX_VERSION_CODE > KERNEL_VERSION(5, 6, 0)
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0)
+    /* 6.5 removed the vmas argument from pin_user_pages(). */
+    result = pin_user_pages(addr & PAGE_MASK, page_count,
+                            (flags & VM_WRITE) ? FOLL_WRITE : 0, pages);
+#elif LINUX_VERSION_CODE > KERNEL_VERSION(5, 6, 0)
     result = pin_user_pages(addr & PAGE_MASK, page_count,
-#elif LINUX_VERSION_CODE < KERNEL_VERSION(4, 6, 0)
-    result = get_user_pages(current, current->mm, addr & PAGE_MASK, page_count,
-#else
-    result = get_user_pages(addr & PAGE_MASK, page_count,
-#endif
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(4, 9, 0) || defined(CONFIG_PPC)
                             (flags & VM_WRITE) ? FOLL_WRITE : 0,
 #else
                             (flags & VM_WRITE) ? 1 : 0, 0,
 #endif
                             pages, NULL);
+#elif LINUX_VERSION_CODE < KERNEL_VERSION(4, 6, 0)
+    result = get_user_pages(current, current->mm, addr & PAGE_MASK, page_count,
+                            (flags & VM_WRITE) ? 1 : 0, 0, pages, NULL);
+#else
+    result = get_user_pages(addr & PAGE_MASK, page_count,
+                            (flags & VM_WRITE) ? FOLL_WRITE : 0, pages, NULL);
+#endif
 
     up_read(&current_mm_mmap_sem);
 
diff --git a/drivers/galcore/os/gc_hal_kernel_driver.c b/drivers/galcore/os/gc_hal_kernel_driver.c
index 264c91c..7bfae27 100644
--- a/drivers/galcore/os/gc_hal_kernel_driver.c
+++ b/drivers/galcore/os/gc_hal_kernel_driver.c
@@ -913,7 +913,11 @@ gceSTATUS viv_device_node_create(uint32_t dev_index)
             major = result;
 
         /* Create the device class. */
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 4, 0)
+        gpu_class = class_create(CLASS_NAME);
+#else
         gpu_class = class_create(THIS_MODULE, CLASS_NAME);
+#endif
 
         if (IS_ERR(gpu_class)) {
             gpu_class = gcvNULL;
diff --git a/drivers/galcore/os/gc_hal_kernel_os.c b/drivers/galcore/os/gc_hal_kernel_os.c
index 448b40a..7bed38e 100644
--- a/drivers/galcore/os/gc_hal_kernel_os.c
+++ b/drivers/galcore/os/gc_hal_kernel_os.c
@@ -417,6 +417,28 @@ _DestroyIntegerId(gcsINTEGER_DB_PTR Database, gctUINT32 Id)
     return gcvSTATUS_OK;
 }
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6, 5, 0)
+/*
+ * Port note (6.6.98-4-aw2511): this kernel does not export
+ * __pte_offset_map_lock(), so pte_offset_map_lock() cannot be used by a
+ * module. Open-code the 5.15-equivalent using static-inline helpers only.
+ * Callers have already validated pmd_none()/pmd_bad().
+ */
+static inline pte_t *
+_galcore_pte_offset_map_lock(struct mm_struct *mm, pmd_t *pmd,
+                             unsigned long addr, spinlock_t **ptlp)
+{
+    pte_t *pte = pte_offset_kernel(pmd, addr);
+
+    *ptlp = pte_lockptr(mm, pmd);
+    spin_lock(*ptlp);
+
+    return pte;
+}
+#define pte_offset_map_lock(mm, pmd, addr, ptlp) \
+    _galcore_pte_offset_map_lock((mm), (pmd), (addr), (ptlp))
+#endif
+
 static inline gceSTATUS
 _QueryProcessPageTable(gctPOINTER Logical, gctPHYS_ADDR_T *Address)
 {
@@ -427,7 +449,7 @@ _QueryProcessPageTable(gctPOINTER Logical, gctPHYS_ADDR_T *Address)
         /* vmalloc area. */
         *Address = page_to_phys(vmalloc_to_page(Logical)) | offset;
         return gcvSTATUS_OK;
-    } else if (virt_addr_valid(logical)) {
+    } else if (virt_addr_valid((void *)logical)) {
         /* Kernel logical address. */
         *Address = virt_to_phys(Logical);
         return gcvSTATUS_OK;
